import { ControlWithDetails } from './types';

// Mock data removed — controls are now loaded from the server via /v2/explorer/controls/search
export const MOCK_CONTROLS: ControlWithDetails[] = [];
